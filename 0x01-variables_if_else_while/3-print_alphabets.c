#include <stdio.h>

/**
 * main - A program that prints lowercase and uppercase
 * Return:0 success
 */

int main(void)
{
	int vat;
	for (vat = 'a';vat <= 'Z';vat++)
	{
		putchar(vat);
	}
	return (0);
}
